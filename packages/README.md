# Shared packages

This directory is reserved for versioned, independently testable packages shared by
multiple applications. Code should remain within its owning feature until a concrete
second consumer exists.

